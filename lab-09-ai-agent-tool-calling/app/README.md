# App Layer

This folder contains Lambda handlers and shared utilities for Lab-09.

## Folders
- `agent_gateway/` - Agent runtime that plans tool calls, executes actions, and returns structured output.
- `authorizer/` - Token-based Lambda Authorizer for API Gateway.
- `shared/` - Bedrock client, tool registry, HTTP utilities, timing, and logging.
